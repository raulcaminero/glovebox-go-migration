services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: glovebox
      POSTGRES_PASSWORD: glovebox
      POSTGRES_DB: glovebox
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
